import { DestacadorDirective } from './destacador.directive';

describe('DestacadorDirective', () => {
  it('should create an instance', () => {
    const directive = new DestacadorDirective();
    expect(directive).toBeTruthy();
  });
});
